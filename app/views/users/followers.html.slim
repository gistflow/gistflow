- title "#{@user}'s followers"
= render :partial => 'sidebar', :locals => { :user => @user }

- if @users.any?
  = render :partial => 'user', :collection => @users
- else
  p.no_followers No followers yet.