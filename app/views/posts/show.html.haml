.post.detail.markdown
  %h1= @presenter.title
  .body= @presenter.body

=caption 'Comments'

- if user_signed_in?
  = form_for @presenter.post.comments.build, :url => post_comments_path(@presenter.post) do |f|
    .form-item.no-margin
      = f.text_area :content, :size => '40x2', :id => 'post_body'
    %ul.left
      = f.check_box :question
      = f.label :question, 'Is it a Question?'
    %ul.button-group
      %li= f.submit 'Comment', :class => 'button'
    .clear

= render @presenter.comments
