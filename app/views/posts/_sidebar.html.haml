- if user_signed_in?
  = caption 'Remembrance'
  %ul
    - sidebar_presenter.posts.each do |post|
      %li
        %b= link_to post.user.username, user_path(:id => post.user.username)
        = link_to post.link_name, post_path(:id => post.id)
