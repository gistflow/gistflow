= simple_form_for(@post) do |f|
  = f.error_notification
  .inputs
    = f.input :title
    = f.input :body
  .actions
    = f.button :submit

.gists
  %ul
    - @gists.each do |gist|
      %li 
        = link_to gist["description"], gist_js_url(gist["id"])